<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/output.css">
    <title>Add home</title> -->
    <%- include('../partials/head', {pageTitle:editMode?'Edit Home':'Add Home'})%>

</head>
<body class="min-h-screen bg-gray-50">
    <!-- <header class="bg-red-400 shadow-md">
        <div class="p-4 px-7 flex items-center text-white">
            <a class="mr-4 hover:text-red-100 transition-colors" href="/host/add-home">Add home</a>
            <a class="hover:text-red-100 transition-colors" href="/">Back To home</a>
        </div>
    </header> -->
    <%- include('../partials/nav') %>
    <main class="container mx-auto px-4 py-8">
        <div class="max-w-2xl mx-auto">
            <div class="bg-white rounded-lg shadow-xl p-8">
                <h1 class="text-2xl font-semibold text-center text-gray-800 mb-8"><%= editMode ? 'Edit' : 'Register' %> Your Property</h1>
                <form action="<%= editMode ? '/host/edithome' : '/host/add-home' %>"  enctype="multipart/form-data" method="POST" class="space-y-4">

                   
                    <div>
                        <input type="hidden" name="id" value="<%= editMode? home._id:'' %>"/>
                        <label for="houseName" class="block text-sm font-medium text-gray-700 mb-2">House Name</label>
                        <input type="text" 
                        id="houseName"
                         name="houseName"
                          required
                          value="<%= editMode? home.houseName:'' %>"
                               class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-red-400 focus:border-red-400 transition-colors"
                               placeholder="Enter your house name"/>
                    </div>

                   
                    <div>
                        <label for="location" class="block text-sm font-medium text-gray-700 mb-2">Location</label>
                        <input type="text" id="location" name="location" value="<%= editMode? home.location:'' %>" required
                               class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-red-400 focus:border-red-400 transition-colors"
                               placeholder="Enter property location"/>
                               
                    </div>

                    
                    <div>
                        <label for="pricePerNight" class="block text-sm font-medium text-gray-700 mb-2">Price per Night ($)</label>
                        <input type="number" id="pricePerNight" name="pricePerNight" value="<%= editMode? home.pricePerNight:'' %>" required min="0"
                               class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-red-400 focus:border-red-400 transition-colors"
                               placeholder="Enter price per night"/>
                               
                    </div>

                    <!-- Rating -->
                    <div>
                        <label for="rating" class="block text-sm font-medium text-gray-700 mb-2">Rating (1-5)</label>
                        <input type="number" id="rating" name="rating" value="<%= editMode? home.rating:'' %>" required min="1" max="5" step="0.1"
                               class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-red-400 focus:border-red-400 transition-colors"
                               placeholder="Enter the rating"/>
                    </div>

                    
                    <div>
                        <label for="Uploadphoto" class="block text-sm font-medium text-gray-700 mb-2">Upload Photos</label>
                        <input type="file" name="photo" id="photo" class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-red-400 focus:border-red-400 transition-colors" accept="image/*" multiple required>
                    </div>

                    <!-- Description -->
                    <div>
                        <label for="description" class="block text-sm font-medium text-gray-700 mb-2">Description</label>
                        <textarea id="description" 
                                  name="description" 
                                  rows="4" 
                                  required
                                  class="w-full px-4 py-2 border border-gray-300 rounded-md focus:ring-red-400 focus:border-red-400 transition-colors"
                                  placeholder="Describe your property"><%= editMode ? home.description : '' %></textarea>
                    </div>
                    
                    <div>
                        <button type="submit" 
                                class="bg-red-400 text-white px-6 py-2 rounded-md hover:bg-red-500 transition-colors focus:outline-none focus:ring-2 focus:ring-red-400 focus:ring-offset-2">
                                <%= editMode ? 'Update' : 'Register' %> Property
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </main>
</body>
</html>